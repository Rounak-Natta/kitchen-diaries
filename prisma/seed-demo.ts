/**
 * Stable demo-seed entry point.
 *
 * `prisma/seed.ts` contains the actual seed implementation; this file keeps
 * the public `seed:demo` command stable without maintaining a duplicate seed.
 */
import "./seed";
